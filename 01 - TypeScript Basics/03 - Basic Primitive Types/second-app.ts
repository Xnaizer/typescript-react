// TypeScript memiliki berbagai tipe data bawaan, termasuk string, number, dan boolean.

// String → Menyimpan teks ("Hello", 'World').
// Number → Menyimpan angka (25, 3.14).
// Boolean → Menyimpan nilai true atau false.

// TypeScript dapat menebak (infer) tipe secara otomatis, tetapi kita juga bisa menentukannya secara eksplisit untuk memastikan kode lebih jelas dan aman.

let userName: string = 'xnaizer';
let age: number = 34;
let isMarried: boolean = false;




// 1. Explicit Type untuk Number dan Boolean

// let age: number = 25;
// let isStudent: boolean = true;

// // age = "twenty-five"; // ❌ Error: Type 'string' is not assignable to type 'number'